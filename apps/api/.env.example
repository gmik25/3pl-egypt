# apps/api .env.example — see repo-root .env.example for full set
# Prisma reads DATABASE_URL from process env; copy the root .env or symlink it.
DATABASE_URL=postgresql://threepl:threepl_dev_pwd@localhost:5432/threepl_egypt?schema=public
